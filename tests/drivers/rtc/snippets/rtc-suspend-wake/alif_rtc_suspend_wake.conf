CONFIG_TEST_RTC_SUSPEND_WAKE=y
CONFIG_PM=y
CONFIG_PM_POLICY_DEFAULT=y
CONFIG_IDLE_STACK_SIZE=1024
# Off/run profiles must not be applied from the idle-thread PM notifier
# (MHU mutex). The test programs them from the test thread / PRE_KERNEL_1.
CONFIG_ALIF_SE_DTS_RUN_PROFILE=n
CONFIG_ALIF_SE_DTS_OFF_PROFILE=n
