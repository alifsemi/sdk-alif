# HE S2RAM STANDBY after A32 is off (same path as cam_pm).
# sys_poweroff() does not go through PM notifiers, so SE never applies
# off_profile_standby and es1_ppu stays ON.

CONFIG_PM_DEVICE=y
CONFIG_POWER_DOMAIN=y
CONFIG_PM_S2RAM=y
CONFIG_PM_SAU_SAVE_RESTORE=y
