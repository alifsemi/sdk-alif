# Video capture pool: JPEG_CAPTURE_MAX_BYTES (688128 = 672K, main.c) plus
# 1536 B of k_heap bookkeeping. Lives in the NON_SECURE0 region (.overlay).
CONFIG_VIDEO_BUFFER_POOL_SZ_MAX=689664
CONFIG_VIDEO_BUFFER_POOL_NUM_MAX=1
